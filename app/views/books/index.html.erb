<h1>Books</h1>

<table>
<% @books.each do |book| %>
  <div class="book-box">
    <tr>
      <th>Title</th>
        <%=book.title %>
      <th>Body</th>
        <%=book.body %>
    </tr>
    <tr>
      <td>
        <span class="book-title">

        </span>
      </td>
      <td>
        <span class="book-body">

        </span>
      </td>
      <td>
        <span>
          <%= link_to Show, show_books_path(book.id) %>
        </span>
      </td>
      <td>
        <span>
          <%= link_to 'Edit', edit_books_path(book.id) %>
        </span>
      </td>
      <td>
        <span>
          <%= link_to 'Destroy', destroy_books_path(book.id) %>
        </span>
      </td>
    </tr>
  </div>
</table>
  
<% end %>

<h2>New Book</h2>

<%= form_with model: @book do |f| %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  
  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Create Book' %>
  </div>
<% end %>


<!--<%= form_with model: @book do |f| %>-->
<!--  <h4>Title</h4>-->
<!--  <%= f.text_field :title %>-->
<!--  <h4>Body</h4>-->
<!--  <%= f.text_area :body %>-->

<!--  <%= f.submit 'Create Book' %>-->
  
<!--<% end %>-->